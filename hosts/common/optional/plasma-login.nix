{
  services.displayManager.plasma-login-manager = {
    enable = true;

    # See: https://invent.kde.org/plasma/plasma-login-manager/-/blob/master/src/common/mainconfig.kcfg
    # for available options.
    # Type: attrs of INI file
    settings = { };
  };
}
